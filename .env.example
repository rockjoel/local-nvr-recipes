# Copy to .env and fill in. Do not commit .env.

# Camera IP on the LAN (placeholder — do NOT use these values in production)
FRIGATE_CAMERA_HOST=192.168.1.50
FRIGATE_CAMERA_USER=camera_user
FRIGATE_CAMERA_PASSWORD=change_me
